const Discord = require ('discord.js');

module.exports = {
    name: "cómo-dormiste",
    run: async (client, message, args) => {
        message.channel.send('Ya sabes que yo no duermo... 😿')
    }
}