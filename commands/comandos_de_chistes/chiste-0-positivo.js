const Discord = require ('discord.js');

module.exports = {
    name: "chiste-0-positivo",
    run: async (client, message, args) => {
        message.channel.send(`-¡Rápido, necesitamos sangre!
  -Yo soy 0 positivo.
  -Pues muy mal, aquí se viene a animar.`);
    }
}